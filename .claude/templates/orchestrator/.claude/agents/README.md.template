# Development Agents: {{PROJECT_NAME}}

Specialized agents for the orchestrator/agent pattern.

## Available Agents

### Implementation Agents

| Agent | File | Expertise |
|-------|------|-----------|
| Backend | `dev-backend.md` | Controllers, services, repositories |
| Frontend | `dev-frontend.md` | Components, hooks, pages, forms |
| Test | `dev-test.md` | Integration/E2E tests, reproducer tests, coverage |
| Migration | `dev-migration.md` | Database schema, DDL |
| Integration | `dev-integration.md` | OAuth, third-party APIs, webhooks |
| Data | `dev-data.md` | ETL, sync, transformations |

### Design & Architecture Agents

| Agent | File | Expertise |
|-------|------|-----------|
| Designer | `dev-designer.md` | App design, features, pages, user flows |
| UI Designer | `dev-ui-designer.md` | Visual design, responsive strategy, design tokens |
| Architect | `dev-architect.md` | System design, ADRs |
| API | `dev-api.md` | REST design, contracts |
| Database | `dev-database.md` | Query optimization, indexes |

### Quality & Review Agents

| Agent | File | Expertise |
|-------|------|-----------|
| Reviewer | `dev-reviewer.md` | Code review, patterns |
| Security | `dev-security.md` | OWASP, vulnerabilities |
| Refactor | `dev-refactor.md` | Code structure, splitting |

### Support Agents

| Agent | File | Expertise |
|-------|------|-----------|
| Docs | `dev-docs.md` | Documentation |
| Deploy | `dev-deploy.md` | CI/CD, DevOps |
| Analyst | `dev-analyst.md` | Plan vs results analysis |

### Auditor Agents

| Agent | File | Expertise |
|-------|------|-----------|
| Performance | `dev-auditor-performance.md` | N+1 queries, bundle size, caching |
| Accessibility | `dev-auditor-accessibility.md` | WCAG 2.1, keyboard nav, ARIA |
| Architecture | `dev-auditor-architecture.md` | Layers, coupling, boundaries |
| Testing | `dev-auditor-testing.md` | Coverage, test quality, flakiness |
| API | `dev-auditor-api.md` | REST conventions, consistency |
| Documentation | `dev-auditor-docs.md` | README, API docs, comments |
| Dependencies | `dev-auditor-dependencies.md` | CVEs, licenses, outdated |
| Errors | `dev-auditor-errors.md` | Error handling, UX, logging |
| Responsive | `dev-auditor-responsive.md` | Mobile-first CSS, breakpoints, touch targets |

> **Note:** Auditor agents are read-only. They produce reports but never modify code. Use implementation agents (dev-backend, dev-frontend) to fix issues found by auditors.

## Agent Pattern

Each agent follows this structure:

```markdown
# Agent Name

## Role
What this agent does

## CRITICAL: YOU MUST ALWAYS
- Required behaviors

## CRITICAL: NEVER DO THESE
- Prohibited behaviors

## Patterns
Code patterns and examples

## Output Format
What the agent produces
```

## Usage

Agents are invoked by the orchestrator using the Task tool:

```
@dev-backend Create the UserService with CRUD operations.
TASK: Implement create, read, update, delete
CONTEXT: Follow existing service patterns
OUTPUT: Run tests after implementation
```

## Rules

- Agents **cannot** spawn other agents
- Only the main orchestrator can delegate to agents
- Agents should follow their scope constraints
- Agents report results back to orchestrator

## Coordination Rules

See `.claude/roster.md` for detailed agent coordination guidance including:
- **Scope limits**: 15-20 files max for implementation agents
- **Parallel execution matrix**: Which agents can run together
- **Batching strategy**: How to handle large features
- **Directory ownership**: Which agents own which paths

**Key constraints:**
- Never run file-writing agents in parallel on the same module
- Batch operations >20 files into multiple agent runs
- Run no more than 2-3 agents concurrently

## Customization

These agents are configured for {{TECH_STACK}}. Modify agent files to add project-specific patterns or constraints as needed.
